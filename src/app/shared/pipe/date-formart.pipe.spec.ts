import { DateFormartPipe } from './date-formart.pipe';

describe('DateFormartPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormartPipe();
    expect(pipe).toBeTruthy();
  });
});
